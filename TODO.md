# TODO

- [ ] Create a shared `<Button>` component (outline "Les mer" variant + solid black variant) and migrate the ~30 raw `<Link>`/`<button>` call sites that each carry copy-pasted button CSS (`site-button` grep finds them all). No shared button component exists today.
